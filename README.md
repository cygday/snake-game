copied the github code 

and unzip the .zip file


unzip name.zip

create virtual env

python3 -m venv my-venv

sourc my-venv/bin/activate

pip install pygame

pip install pygbag

then

python3 -m pygbag . for build/web directory to launch it on render as static page

cd build/web
git clone https://www.github.com/cygday/snake-game.git

git add .
git commit -m 'web game'

git remote add origin https://www.github.com/cygday/snake-game.git

git push -u origin main

username cygday

password patnew of desktop

while on render

publich directory to .

deploy as static page

